export const environment = {
  production: false,
  apiUrl: /*process?.env["API_URL"] ||*/ "http://localhost:3000",
  productUrl: /*process?.env["PRODUCT_URL"] ||*/ "/product",
  shopUrl: /*process?.env["SHOP_URL"] ||*/ "/shop",
  authUrl: /*process?.env["AUTH_URL"] ||*/ "/auth",
  categoryUrl: /*process?.env["CATEGORY_URL"] ||*/ "/category",
}